export interface User{
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    spotify_token: string;
    spotify_refresh: string;
    spotify_expiration: string;
    playlistInfo: string;
}